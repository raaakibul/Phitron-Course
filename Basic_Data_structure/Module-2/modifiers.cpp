#include<bits/stdc++.h>
using namespace std;
int main(){
    vector<int> v = {1,2,3,4,5};
    vector<int>v2;
    v2 = v;
    // v.pop_back();
    // v.pop_back();
    // v.insert(v.begin()+2,100);
    // v.erase(v.begin()+2);
    // replace
    // find
    
    for(int x:v2){
        cout << x << " ";
    }
    cout << endl;
    return 0;
}