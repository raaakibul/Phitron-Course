#include <stdio.h>
int main() {
    long long int a,multiplication;
    long long int b;
    scanf("ll%d %lld",&a,&b);

    multiplication = a*b;
    printf("%lld",multiplication);
      
    return 0;
}