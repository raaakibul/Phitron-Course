#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {

    int Abul, Babul,Bablu;
    scanf("%d %d %d",&Abul, &Babul, &Bablu);
    int total = Abul + Babul +Bablu;
    printf("%d",total);
    return 0;
}